bindkey -e '^H' backward-kill-word # ctrl backspace
bindkey -e '^[[3;5~' kill-word # ctrl delete

# Dica: Para saber daonde veio este "^[[3;5~" faça o seguinte:
# Vá no zsh, pressione ctrl v, seguido pela sequência que você quer
# Vai aparecer o código dela, aí você pega-a, e cola aqui
